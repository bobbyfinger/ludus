{
  "name": "ludus",
  "version": "0.1.0",
  "private": true,
  "description": "LUDUS — résurrection de Sang & Sueur : école de gladiateurs PBEM moderne.",
  "type": "module",
  "scripts": {
    "test": "test -n \"$(find src test -name '*.ts' 2>/dev/null)\" && (tsc --noEmit && node --test) || echo 'bootstrap: no TS sources yet'"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0"
  }
}
