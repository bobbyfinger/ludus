{
  "name": "ludus",
  "version": "0.1.0",
  "private": true,
  "description": "LUDUS — résurrection de Sang & Sueur : école de gladiateurs PBEM moderne.",
  "type": "module",
  "scripts": {
    "test": "tsc --noEmit && node --test test/"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
